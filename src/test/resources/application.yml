spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/board
    username: sa
#    password:
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    defer-datasource-initialization: false
    # 로그
#    properties:
#      hibernate:
#        show_sql: false
#        format_sql: false

# 로그 -> 파라미터 값이 여러개 나오는 불편함 -> p6spy 변경
#logging:
#  level:
#    org.hibernate.SQL: DEBUG
#    org.hibernate.orm.jdbc.bind: TRACE
